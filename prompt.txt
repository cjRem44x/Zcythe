Why doesn't this code work?

# entry point
@main {
    @pl("Hello World") # println builtin

    # C++ print format
    @cout << "Hello from Glorbglob\n"

    # Zig/Scala printFormat
    var := "6..7"
    @pf("Hello Pog {var}\n")
}

---
Comment code well, add headers, and update the BuildNotes and other docs freq.
Commit early and commit often.
